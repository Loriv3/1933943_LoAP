{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "api.actuators.post",
    "type": "object",
    "properties": {
        "state": {
            "enum": [
                "ON",
                "OFF"
            ],
            "description": "This actuator's new state."
        }
    },
    "required": [
        "state"
    ]
}
